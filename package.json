{
  "name": "webpack-react-spa",
  "version": "0.0.1",
  "description": "Webpack react SPA for mtt scaffold",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "webpack-dev-server --devtool source-map -d --history-api-fallback --hot --inline --progress --colors --port 3000",
    "build": "set NODE_ENV=production&&webpack --progress --colors -p",
    "mock": "node ./mock-server/server.js 2618",
    "huhai": "webpack"
  },
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.9.1",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.9.0",
    "webpack": "^1.13.1"
  },
  "dependencies": {
    "antd": "^1.2.1"
  }
}
